# Stonkfly

- Preserve the full retained MaleCNS v1.0 graph. No pruning, scripted tile picks presented as neural output, LLM policy, or hidden outcome-based tile selection.
- Separate board observation, sensory proxies, neural propagation, plasticity, fixed decoding, and execution limits. The guard may veto a deploy; it must never choose replacement tiles or a different stake.
- Round-outcome reinforcement is an engineered input to identified dopamine cells. Do not claim modeled pain, pleasure, consciousness, or validated learning.
- Paper play is the default. Never send real deploys while testing. Keypairs, wallet addresses, balances, logs, checkpoints and data stay ignored.
- Use Decimal for money and integer micro-USDC/sats on chain. Persist the signed transaction signature before sending it. Unknown outcomes stop execution until reconciliation; never blindly resend a deploy.
- The SatRush program layout in `stonkfly/satrush/program.py` was reconstructed from the public web client; verify PDAs and layouts against live accounts (tests do) before changing them.
- Keep README short. Detailed model, game and execution caveats belong in docs.
