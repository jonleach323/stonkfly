[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "stonkfly"
version = "0.2.0"
description = "An experimental fly-connectome controller that plays the SatRush tile game on Solana under strict limits"
requires-python = ">=3.11"
dependencies = [
  "numpy==2.4.6", "pandas==3.0.5", "pyarrow==25.0.1", "Pillow==12.3.0",
  "python-dotenv==1.2.3", "solders==0.29.0"
]

[project.optional-dependencies]
test = ["pytest==9.1.1"]

[project.scripts]
stonkfly = "stonkfly.cli:main"

[tool.setuptools.packages.find]
include = ["stonkfly*"]

[tool.setuptools.package-data]
"stonkfly.neural" = ["*.cpp", "*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
