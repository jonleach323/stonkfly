# Solana keypair JSON (the `solana-keygen` array format). Keep it outside git.
# Use a DEDICATED wallet funded with at most 100 USDC and a little SOL for fees.
SATRUSH_KEYPAIR=satrush-keypair.json
# Live mode requires both this exact opt-in and the --live CLI flag.
STONKFLY_LIVE=I_ACCEPT_REAL_DEPLOYS
# Optional overrides
# SATRUSH_RPC_URL=https://rpc.satrush.io
# SATRUSH_API_URL=https://api.satrush.io/api
# STONKFLY_DATA=data
